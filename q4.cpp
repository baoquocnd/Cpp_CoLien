#include <bits/stdc++.h>

using namespace std;

int main(){
    string a,b; 
    getline(cin,a);
    getline(cin,b);
    if (a==b) cout<<"two people have the same name";
    else cout<<"two people don't have the same name";
}
